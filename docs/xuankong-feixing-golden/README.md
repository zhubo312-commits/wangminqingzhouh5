# 玄空飞星参考证据（阻塞）

采集日期：2026-08-12。参考入口为 `https://ft.bavor.cn/#/pages/xuankongfeixing/xuankongfeixing`，结果接口为 `GET /prod-api/app/xkfx/getXkfxH5`。

## 材料

- `source-manifest.json` 固化表单、结果生产脚本 URL 和 SHA-256，并记录三次脱敏响应的字节数与 SHA-256。
- `case-01` 为九运、子山午向、下盘、`2024-02-04 12:00`。
- `case-02` 只将起盘方式改为替盘，用于证明山星、向星确实改变。
- `case-03` 只将时间改为 `2024-02-05 12:00`，用于证明立春附近的农历干支和年/月/日/时飞星改变。
- 每例保存脱敏请求和关键字段摘要；摘要按接口宫序 `1..9`，前端实际按洛书顺序 `4,9,2,3,5,7,8,1,6` 展示。

## 审计结论

旧表单/结果脚本只负责参数收集、接口调用和渲染。核心算法与解释语料位于不透明的线上服务，当前仓库没有对应实现。参考响应不是本地生产运行时依赖，三例也不足以反推九运、二十四山、下盘/替盘和历法边界的完整算法。因此本目录是“可复核证据”，不是“已满足 Definition of Ready 的黄金测试集”。

完整阻塞项见 `docs/玄空飞星页面功能设计.md`。在授权算法补齐前，不创建 Java Engine，不开放 `/paipan/xuankong-feixing`。
