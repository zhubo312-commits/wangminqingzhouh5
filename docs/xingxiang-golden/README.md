# 星像学黄金材料

`source-manifest.json` 固化参考页面、版本化脚本、接口线索、采集时间和 SHA-256。`case-01` 是 Java Engine 的主黄金例；`case-02`、`case-03` 是跨性别、跨年份与子时边界的交叉核验材料。

响应由合成资料采集，并已检查不含 Cookie、Authorization、账号、手机号、真实客户姓名或位置。运行时不读取这些文件，也不调用参考站。
