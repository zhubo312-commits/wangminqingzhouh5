# 后续九种排盘推进清单

版本：1.2<br>
日期：2026-08-12<br>
执行框架：`docs/后续九种排盘开发执行框架.md`<br>
统一设计：`docs/专业排盘统一设计规范.md`

## 1. 状态定义

| 状态 | 含义 | 是否允许编码 |
|---|---|---|
| 待盘点 | 尚未确认算法来源、输入输出和专业结构 | 否 |
| 资料补充中 | 已定位部分来源，但 Definition of Ready 未完成 | 否 |
| 可开发 | 算法来源、脱敏样例、黄金结果和功能设计均已确认 | 是 |
| 开发中 | 单盘分支正在完成纵向链路 | 是 |
| 待验收 | 功能已完成，正在跑完整验证和三档视觉检查 | 仅修复 |
| 已验收 | Definition of Done 全部满足并已形成独立提交 | 已结束 |
| 阻塞 | 缺少必须由用户或外部系统提供的资料/权限 | 否 |

状态只能按证据更新，不能因为页面能打开就改成“已验收”。

## 2. 已完成基线

| 盘种 | 路由 | 契约版本 | 当前状态 | 说明 |
|---|---|---|---|---|
| 生平子时 | `/paipan/shengping-zishi` | `guoxue.paipan.bazi.v1` | 已验收 | 已有表单、结果、上下文恢复、老师入口与三档 E2E |
| 遁甲学 | `/paipan/dunjia` | `guoxue.paipan.dunjia.v1` | 已验收 | 已有本地 Java 算法、表单、九宫结果、上下文恢复与三档 E2E |

本次开发基线：`main@1c0626c`。开始新任务时仍需重新核对，因为本表记录可能落后于仓库。

## 3. P0 公共底座清单

状态：`已验收`（本地 `main@d927558`）

- [x] 对比生平子时和遁甲学，记录真正重复的页面骨架、状态和交互。
- [x] 抽离 `PaipanPageShell`。
- [x] 抽离 `PaipanSectionCard`。
- [x] 抽离 `InfoGrid` / `InfoPair`。
- [x] 抽离 `InlineSelectionGrid`，保留选择、收起、指向气泡和无障碍语义。
- [x] 抽离 `PaipanEmptyState`。
- [x] `FiveElementLabel` 与盘种表格未满足双盘复用条件，保持盘种内实现。
- [x] 将排盘上下文改为可扩展的版本化存储与按盘 Schema 注册/解析。
- [x] 保持生平子时和遁甲学接口、恢复、过期及错误语义兼容。
- [x] 为公共底座补单元测试、数据保留迁移和旧盘种回归。
- [x] Node typecheck/test/build 通过。
- [x] Java `mvn verify` 通过。
- [x] 360/390/430 三档 E2E 通过。
- [x] 公共底座形成独立提交 `d927558`，并快进合并到本地 `main`。

P0 未达到“已验收”前，不开始剩余九种排盘的并行开发。

## 4. 九种排盘总表

下表中的 `slug` 与路由只是工程建议，不代表已确认算法映射。尤其是“决策学、逻辑学、星像学”等产品名称，必须先核实其真实算法和旧系统对应关系。

| 编号 | 用户名称 | 建议 slug | 建议路由 | 算法来源 | 脱敏请求/响应 | 黄金结果 | 功能设计 | 当前代码 | 状态/阻塞 |
|---|---|---|---|---|---|---|---|---|---|
| P01 | 决策学 | `juece` | `/paipan/juece` | `ft.bavor.cn` 当前星河转盘与飞盘生产脚本，SHA-256 已固化 | 六类完整材料 | 六类差异 0 | `docs/决策学页面功能设计.md` | 全链路与入口已完成 | 已验收：参考站一致 |
| P02 | 阴盘决策 | `yinpan-juece` | `/paipan/yinpan-juece` | 参考站当前阴盘生产脚本与仓库冻结同源算法，SHA-256 已固化 | 一份完整材料 | 一份关键字段一致 | `docs/阴盘决策页面功能设计.md` | 全链路与入口已完成 | 已验收：参考站一致 |
| P03 | 梅花学 | `meihua` | `/paipan/meihua` | 待确认 | 无 | 无 | 无 | 仅导航禁用项 | 待盘点 |
| P04 | 逻辑学 | `luoji` | `/paipan/luoji` | 待确认 | 无 | 无 | 无 | 仅导航禁用项 | 待盘点：不可凭产品名映射传统盘种 |
| P05 | 星像学 | `xingxiang` | `/paipan/xingxiang` | 待确认 | 无 | 无 | 无 | 仅导航禁用项 | 待盘点：需确认“星像”命名及数据范围 |
| P06 | 姓名学 | `xingming` | `/paipan/xingming` | 待确认 | 无 | 无 | 无 | 仅导航禁用项 | 待盘点：需确认字库、笔画与异体字规则 |
| P07 | 数字规律 | `shuzi-guilv` | `/paipan/shuzi-guilv` | 待确认 | 无 | 无 | 无 | 仅导航禁用项 | 待盘点：需确认数字输入与计算规则 |
| P08 | 山向决策 | `shanxiang-juece` | `/paipan/shanxiang-juece` | 待确认 | 无 | 无 | 无 | 仅导航禁用项 | 待盘点：需确认方位、坐向与时间条件 |
| P09 | 玄空飞星 | `xuankong-feixing` | `/paipan/xuankong-feixing` | 待确认 | 无 | 无 | 无 | 仅导航禁用项 | 待盘点：需确认运盘、山盘、向盘规则 |

当前仓库中 P01、P02 已完成公开 REST 契约、Fastify 路由、Java Engine/Controller、前端页面和普通构建入口。P03–P09 仍没有完整纵向链路，不得把导航禁用项误报为已开发。

## 5. Definition of Ready

一个盘种只有以下项目全部完成，状态才能从“待盘点/资料补充中”改为“可开发”：

- [ ] 用户名称、真实算法名称和旧系统对应关系已确认。
- [ ] `slug`、路由、`chartType` 和 Schema 版本已确认。
- [ ] 授权算法来源、文件/接口位置和版本/提交号已记录。
- [ ] 原始页面及全部必要子页面可访问，版本化脚本、采集时间和 SHA-256 已记录。
- [ ] 已实际操作全部起盘方式、条件显隐和结果工具，原始功能清单已保存。
- [ ] 新旧功能对照矩阵已完成，每项均标记为原样保留、等价重构、经确认移除或阻塞。
- [ ] 至少一份脱敏合法请求已保存。
- [ ] 对应原始响应已保存，字段含义已说明。
- [ ] 每种起盘模式至少一份可与可信来源核对的黄金结果已确认。
- [ ] 必填、选填、默认值、范围、互斥条件和错误边界已列出。
- [ ] 专业阅读路径已写出。
- [ ] 每组信息的显示层级已标注。
- [ ] 模块合并/拆分理由已说明。
- [ ] 智能老师入口需求已明确；默认结论为不提供。
- [ ] `docs/<盘种>页面功能设计.md` 已完成并确认。

## 6. 单盘开发清单模板

开始 P01–P09 中任意一项时，将盘种编号和名称填入标题，并逐项勾选。

### `[编号] [盘种名称]`

状态：`待盘点`<br>
分支：`feat/paipan-<slug>`<br>
负责人/执行模型：<br>
开始日期：<br>
验收日期：<br>
提交：<br>

#### A. 资料与设计

- [ ] Definition of Ready 全部满足。
- [ ] 功能设计文档已加入 `docs/`。
- [ ] 原始页面、子页面、脚本版本、采集时间、SHA-256 和关键截图已固化。
- [ ] 原始功能清单覆盖全部起盘模式、字段、默认值、条件显隐、结果模块和结果工具。
- [ ] 新旧功能对照矩阵无空白项；移除项有确认依据，阻塞项保持入口关闭。
- [ ] 脱敏请求、响应及黄金结果放在合适的测试 fixture 目录。
- [ ] 专业阅读路径与旧接口字段顺序明确区分。
- [ ] 非目标和不展示的算法中间数据已记录。

#### B. 契约与算法服务

- [ ] Zod 请求 Schema。
- [ ] Zod 响应 Schema。
- [ ] 上下文响应 Schema 与版本号。
- [ ] `openapi.yaml` 同步。
- [ ] Java Models、Engine、Controller。
- [ ] Engine 黄金用例测试。
- [ ] Controller 正常与错误测试。

#### C. Fastify 与上下文

- [ ] Java Client 调用与超时/错误处理。
- [ ] 公开 chart 路由。
- [ ] 上下文创建与恢复路由。
- [ ] `paipan_ref` 哈希存储与 TTL。
- [ ] 盘型/版本不匹配拒绝恢复。
- [ ] Client、Route、Context 单元测试。

#### D. 前端

- [ ] Session Provider 与独立 `sessionStorage` key。
- [ ] 表单页、字段校验和提交状态。
- [ ] 结果页按专业阅读路径组织。
- [ ] 原始页面全部起盘方式、选项、默认行为和条件显隐均已原样保留或等价重构。
- [ ] 原始结果模块、详情层级、上一/下一、切换、联动、展开、放大和重排等工具均已覆盖。
- [ ] 加载、空态、错误、重试和过期状态。
- [ ] 直接刷新恢复盘面。
- [ ] 结果返回表单，表单返回排盘导航。
- [ ] 复用公共组件，未复制公共样式实现。
- [ ] 无整页横向溢出，触控与无障碍语义合格。
- [ ] 非生平子时盘种未出现智能老师入口。

#### E. 开放与验收

- [ ] `App.tsx` 路由加入。
- [ ] 该盘种 `catalog.ts` 改为 `enabled: true`，其余未完成项不变。
- [ ] `git diff --check` 通过。
- [ ] `npm run typecheck` 通过。
- [ ] `npm test` 通过。
- [ ] `npm run build` 通过。
- [ ] `npm run test:e2e` 三档宽度通过。
- [ ] `services/paipan` 下 `mvn verify` 通过。
- [ ] 360px 表单与结果图已检查。
- [ ] 390px 表单与结果图已检查。
- [ ] 430px 表单与结果图已检查。
- [ ] 长文本、大字号、安全区、错误和恢复场景已检查。
- [ ] 功能对照矩阵已逐项执行，没有“未实现但未记录”的原始功能。
- [ ] 每种起盘模式和关键条件组合的黄金结果均通过；所有字段差异均可解释。
- [ ] 功能设计文档和本清单已更新。
- [ ] 独立提交已形成，已知风险和未做事项已记录。

## 7. P01 决策学实际记录

状态：`已验收`<br>
分支：`feat/paipan-juece`<br>
算法标识：`shijia_juece`<br>
Schema：`guoxue.paipan.shijia_juece.v1`<br>
功能提交：`d7608a4`<br>
开放提交：本次参考站对齐与开放提交

已完成：

- [x] 参考站四个版本化生产脚本与旧包四个关键文件 SHA-256 已记录。
- [x] 六类请求、参考站原始结果、旧算法迁移结果、新版标准化结果和逐字段差异已生成。
- [x] Zod 请求/响应/上下文 Schema 与 OpenAPI 已同步。
- [x] Java Engine、Controller、真太阳时、参考站同源转盘和飞盘全部组合已实现。
- [x] Fastify Java Client、chart/context 路由、引用哈希和严格恢复已实现。
- [x] 三段式表单、九宫盘、行内详情、前后时辰、恢复/过期/重试已实现。
- [x] 当前标签页独立 `sessionStorage` key 已实现。
- [x] 智能老师、旧断语和旧算法日志未进入功能或响应。
- [x] `git diff --check`、类型检查、Node 单测 34 项、生产构建通过。
- [x] 360/390/430 三档 E2E 27 项通过。
- [x] Java `mvn verify` 18 项通过。
- [x] `docs/previews/juece-form-{360,390,430}.png` 已人工检查。
- [x] `docs/previews/juece-result-{360,390,430}.png` 已人工检查。
- [x] 六例 `docs/juece-golden/case-XX/result-390.png` 已人工检查。
- [x] 参考站转盘固定“拆补·时空·寄坤”的真实行为已在页面明确说明。
- [x] 参考站 1899–2101 年节气秒值表已固化，六组结果逐字段差异均为 0。
- [x] 普通构建已注册决策学路由，`catalog.ts` 已设置 `enabled: true` 与 `/paipan/juece`。
- [x] 业务方已明确“与参考网址结果一致即通过”，无需额外教师签认。

最终验收已完成：`git diff --check`、类型检查、Node 单测 34 项、生产构建、三档 E2E 27 项、Java `mvn verify` 18 项及三档预览重建均通过。本次只形成本地提交，不推送、不部署。

## 8. P02 阴盘决策实际记录

状态：`已验收`<br>
分支：`feat/paipan-yinpan-juece`<br>
算法标识：`yinpan_juece`<br>
Schema：`guoxue.paipan.yinpan_juece.v1`<br>
提交：本次 P02 独立提交

已完成：

- [x] 参考站六个版本化生产脚本 URL 与 SHA-256 已记录。
- [x] 无姓名、无事项的脱敏请求、参考站原始响应和关键黄金结果已固化。
- [x] 时盘、刻盘、性别、30 字事项和可选终身局的输入边界已确认。
- [x] 上一/下一局、上一/下一日、九宫详情、天门地户、放大和重排已覆盖。
- [x] Zod 请求/响应/上下文 Schema 与 OpenAPI 已同步。
- [x] Java Models、Engine、Controller 和黄金/校验测试已完成。
- [x] Fastify Client、chart/context 路由、引用哈希、TTL 与严格恢复已完成。
- [x] Session Provider、移动端表单、结果页、空态、错误和刷新恢复已完成。
- [x] 普通构建已注册路由，导航入口已设置 `enabled: true`。
- [x] 智能老师和个案断语未进入页面或响应。
- [x] `git diff --check`、类型检查、Node 单测 41 项、生产构建通过。
- [x] Java `mvn verify` 22 项通过。
- [x] 完整 360/390/430 E2E 30 项通过，其中阴盘闭环 3 项。
- [x] `docs/previews/yinpan-form-{360,390,430}.png` 已人工检查。
- [x] `docs/previews/yinpan-result-{360,390,430}.png` 已人工检查。
- [x] 黄金例四柱、阴遁 9 局、旬首、空亡、值符、值使、月将、马星、九宫及 12 组天门地户与参考站一致。

保留的兼容行为：刻盘“上一日/下一日”沿用参考站现状按两小时移动，已在功能设计中明确记录。

## 9. 推荐排期规则

当前不根据名称直接给 P01–P09 排固定开发顺序。先完成资料盘点，再按以下顺序选择：

1. 算法来源已授权且能够本地复用。
2. 有脱敏请求、原始响应和可信黄金结果。
3. 专业阅读路径已由业务方确认。
4. 能验证公共底座，但不是结构极端的特殊盘种。
5. 对其他盘种复用价值较高、依赖较少。

如果多个盘种同时达到“可开发”，优先选输入输出边界最清楚、黄金结果最容易核验的一项。高风险或资料含糊的盘种保持“待盘点/阻塞”，不能为了排期强行开发。

## 10. 清单更新规则

每次任务结束都必须更新：

- 状态。
- 分支与提交。
- 已完成复选框。
- 测试数量与结果。
- 三档预览图路径。
- 已知风险、未做事项和阻塞负责人。

只允许根据实际文件、命令输出和人工验收更新。计划、截图草稿、旧系统页面或模型自述不能作为“已验收”的依据。
