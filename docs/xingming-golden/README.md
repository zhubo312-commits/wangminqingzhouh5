# 姓名学参考证据与样例

采集日期：2026-08-12。参考入口为 `https://ft.bavor.cn/#/pages/xingming/xingming`，当前生产计算接口为 `GET /prod-api/app/getPartialAnalysis`。

## 材料

- `source-manifest.json` 固化应用入口、五格/六格页面脚本、接口位置及 SHA-256。
- `case-wuge/reference-request.json` 与 `expected-summary.json` 固化三才五格脱敏样例。
- `case-liuge/reference-request.json` 与 `expected-summary.json` 固化三才六格脱敏样例，并覆盖复姓和双字名。
- 原始响应含大段逐字字典释义，本次只入库页面实际使用的稳定字段；原始响应整体 SHA-256 记录在 manifest，避免把无关长篇字典内容误作公开契约。

## 已确认行为

- 表单限制：姓 1–2 字；五格名字 1–3 字；六格名字 1–2 字。
- 页面跳转只携带姓、名和流派。性别未传入；生日由结果页固定为 `1996-12-12 00:00:00`，`sex` 固定为 `1`。
- 同一脱敏姓名使用 `2001-01-01 12:34:00` 与 `sex=0` 对照后，响应 `data` 与固定参数逐字段相同。
- 日期、地区、真太阳时只作为未使用状态残留在表单脚本中，没有控件，也没有进入请求。
- 运行时不得访问参考站。当前仓库缺少完整本地字表与算法来源，因此这些样例仅用于资料冻结，不能作为只支持少数字符的“算法”。

## 当前结论

两种流派均已获得可复核样例，但 Definition of Ready 未完成。缺少康熙字表、繁简/拼音/笔画/五行口径、81 数理、三才组合、五格/六格公式和评分规则；P06 保持阻塞且导航关闭。
